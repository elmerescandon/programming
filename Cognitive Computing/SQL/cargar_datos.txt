create database covid;
use covid;

drop table positivos;

create table positivos(
uuid 			 varchar(40),
departamento 		 varchar(30),
provincia 		 varchar(30),
distrito 		 varchar(30),
metodox 		 varchar(5),
edad 			 varchar(5),
sexo 			 varchar(10),
fecha_resultado   	 varchar(20)
); 
load data infile 'c:\\covid_clase_csv.csv' into table positivos fields terminated by ',' lines terminated by '\n';